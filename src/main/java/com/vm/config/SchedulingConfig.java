package com.vm.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SchedulingConfig {
    // This class is kept for future scheduling configurations if needed
    // Quartz is now handling all scheduling
}
